slots:
  # === 查询物流 ===
  logistics_id: # 物流ID
    type: text
    mappings:
      - type: controlled

actions:
  # === 查询承运商 ===
  - action_get_logistics_companys # 获取支持的承运商
  - action_get_logistics_info # 获取物流信息