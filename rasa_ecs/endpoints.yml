action_endpoint:
  actions_module: "actions"  # 添加这一行
nlg:
  type: rephrase
  llm:
    model_group: qwen

model_groups:
  - id: qwen
    models:
      - provider: self-hosted
        model: qwen-max
        api_base: "https://dashscope.aliyuncs.com/compatible-mode/v1"
        api_key: ${API_KEY}